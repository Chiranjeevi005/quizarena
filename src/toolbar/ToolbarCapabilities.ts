export interface ToolbarCapabilities {
  groups: boolean;
  actions: boolean;
  overflow: boolean;
  commandBar: boolean;
  actionBar: boolean;
  responsive: boolean;
  accessibility: boolean;
  compactMode: boolean;
}
