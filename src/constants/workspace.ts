/**
 * QuizArena — Workspace Constants
 *
 * Shared configuration for administrative workspaces.
 */

// Minimum viewport width in pixels required to access admin and super-admin dashboards.
// Represents the "lg" breakpoint typically (1024px) to ensure accurate moderation and operations.
export const ADMIN_MIN_WIDTH = 1024;
