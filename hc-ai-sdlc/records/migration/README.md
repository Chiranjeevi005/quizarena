# Migration Records

This directory contains the Migration Manifest (`MIG-001`) and historical records of framework migrations. These records act as the official ledger for documents transitioned from legacy versions of the EOS into the current HC-AI SDLC framework.
